# ast-grep project: code-shape rules that import graphs and Biome cannot express.
# Run through `yarn ast-grep scan`; rule tests run with `yarn ast-grep test`.
ruleDirs:
  - scripts/static/ast-grep/rules
testConfigs:
  - testDir: scripts/static/ast-grep/tests
