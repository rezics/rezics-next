# G-024 — atomic independent-organization Realm transfer

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 41 |
| State | Integrated and affected-verified on `main` as `775185c`; IAM24 declared a complete-case candidate pending final recorded backend QA. Worker task `01a0dcfa-8fa2-7571-b347-e4c2699894b6` finished. |
| Base | Clean dispatch `main` at `42d5408` (dispatch documentation only above `c18c01b`), worktree `/home/edge/.codex/worktrees/b727/rezics-next`; G-022 integration and G-023 package work remain manager-sequenced separately |
| Deliverable | Add an explicit, single-transaction move of one independent organization's admitted participation from a source Realm to a target Realm. The organization opts to leave the exact source episode and accepts an unexpired, independent two-party target proposal. The source history advances to left and target history to joined under one Access authority epoch, with one immutable idempotent receipt. Neither structural move nor any descriptive link creates organization management, source ownership or cross-Realm editing power. |
| Acceptance | Seek a complete IAM24 candidate only after combining G-012's independent join, G-017's explicit managed grant, and this real move on merged source. Preserve IAM23 local moderation and its finite in-flight decision guarantee. Broader Realm quotas, review, paid benefits, founding grants and organizational control remain separate. Final recorded backend QA decides qualification. |
| Dependencies | G-012 Access Org/Realm proposal/change owner, G-017 managed-org grants and G-020 moderation saved episodes. Apply `api-ui-design` and `research-and-validation` before wire/schema. Design source and target tuple/history identity, proposal use, immutable paired move receipt, recovery coverage and indexed cost before route edits. The current `join`/`leave` calls are independent and can pass through an intermediate state; do not label them an atomic move. |
| Owned paths | Access Org/Realm owner and migration, narrow Main Access route/schema, recovery coverage, selected real Account/Main/Access API and Access WAL tests, Realm/IAM contracts and testing docs. Avoid Rating, npm package and Jena model owner changes. Manager sequences shared `app.ts` and generated OpenAPI. |
| Checks | Write tests with implementation. A current organization representative with exact `access.org-realm.participate` authority moves only its own joined source episode, using a distinct currently authorized target Realm manager's unused proposal. A stale source or target generation/policy, expired/revoked proposal proof, ban, closed target, same Realm, self-approval, inactive Account/Access principal, lock-wait expiry or held recovery denies both effects. Concurrent moves and retries produce one paired history and one receipt; changed intent conflicts. Source becomes left, target joined, unrelated Realm, roster, representation, grants, managed grant and native Work remain unchanged. A source Realm moderation request cannot acquire a new G-020 admission after move, while already dispatchable work retains only its original finite deadline. Prove exact read/replay after isolated Access WAL restore and bound indexed probes/selected rows/writes under unrelated history growth. Run documented affected `yarn test` paths, `yarn gen`, `yarn check:backend`, `yarn docs:check`; manager repeats merged checks. |
| Estimate | Initial 180–240 minutes after owner schema and target-proposal proof design; report any authority or lock-order ambiguity before broadening scope. |
| Handoff | Commit one coherent independent worktree result with commit, exact checks/artifacts, unresolved IAM24 boundaries and generated-file note, then finish. No remote push or full backend QA. |

The Access global participation gate already serializes transitions; the move
must still lock and validate both exact tuple/proposal branches and commit or
roll back their effects together. It must not infer managed mode from Realm
membership. Dispatch only when a worker slot is free and the then-current base
has passed merged checks.

Worker result, 2026-09-26: migration `028_org_realm_move.sql`, one Access
transaction and `POST /v1/access/org-realm-moves` now implement paired leave/join,
immutable pairing/history/receipt, exact target proof and source episode checks,
and recovery coverage. The migration reserves no `029` path. Eight logical writes
commit together; replay writes none. Both late constraint failure and proposal
expiry after attempted writes roll back the complete move. A return move reuses
the original tuple with a new generation. An explicit parent-management grant
remains usable after moving; a populated roster and unrelated authority/Realm
state remain unchanged. G-020 tests preserve native state, deny new source
admissions and retain an existing admission's original finite deadline.

Checks actually run:

- `yarn install --immutable`, `yarn gen`, `yarn check:backend` and `yarn docs:check`.
- `yarn test tests/qa/integration/access-org-realm-move-api.test.ts tests/qa/integration/access-org-realm-api.test.ts tests/qa/integration/access-managed-organization-api.test.ts tests/qa/integration/organization-publication-moderation.test.ts`:
  run `20260926t092431-149640`, four passes, 562 assertions, 26.3 seconds.
- After strengthening populated-roster/usable-management preservation,
  `yarn test tests/qa/integration/access-org-realm-move-api.test.ts`:
  run `20260926t092718-2c90bf`, one pass, 7.8 seconds.
- `yarn test services/main/tests/access-pitr.integration.test.ts`:
  run `20260926t092432-d9e446`, one pass, 71 assertions, 13.4 seconds.
  Archived WAL restores both histories, consumption, pair and exact receipt;
  incomplete WAL and held authority deny operations.

The local `.artifacts/qa/<run-id>/` directories contain JUnit and acceptance
summaries. Initial failed runs `20260926t092217-7278a9` and
`20260926t092218-a6838c` identified a trigger alias collision and one expected
HTTP status mismatch; both were corrected before the passing selections.
Manager integration must reconcile the narrow `app.ts` change, the generator's
command list/route count and `generated/openapi/main/public.json`, then run merged
checks and decide the IAM24 complete-case mapping with G-012/G-017. This worker
did not change npm, Rating or Jena owner code, push remotely, or run full backend
QA. Cold I/O, global gate throughput, general managed operations, founding grants,
control/recovery, voting, quota/review and paid benefits remain outside this slice.
