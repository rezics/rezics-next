# G-005 — Exact local Go module replacement snapshot

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 3 |
| State | Ready as an independent package slice while G-004 owns Access consent |
| Base | Clean `main` after G-004 dispatch; start in a fresh isolated worktree |
| Deliverable | Add one versioned, bounded Go 1.16 unpruned MVS snapshot profile for main-module local `replace` directives. Accept exact caller-supplied local `go.mod` bytes with a stable source identity and digest, rather than reading an arbitrary host path in the API. Resolve its requirements and selected source meaning without inventing a remote version or checksum for local bytes. Compare build list and replacement identity against the pinned native Go 1.27.1 oracle using a generated local module directory. Distinguish malformed, missing, changed-digest, duplicate/conflicting, unsupported and budget-exhausted outcomes; preserve immutable private API read/replay. |
| Acceptance | Partial PKG05/PKG12 local-replacement assertions. Retraction, current live provider sets, Go 1.17+ pruning and package lock/installations remain in the backlog; this task cannot mark full cases complete. |
| Dependencies | Existing B51–B75 Go MVS v2/v3, `yarn package:go-oracle`, private resolution owner and real PostgreSQL/Main API fixture. Follow the official Go module reference and current `docs/contracts/package-profiles.md`; use the already pinned Go toolchain only. |
| Owned paths | `services/main/src/modules/package/go-mvs.ts`, `scripts/package/go-oracle.ts`, Go MVS unit/integration tests, `docs/testing/packages.md` and package contract if semantics require clarification. The package request/response section of `services/main/src/app.ts` is yours; G-004 owns separate membership sections. |
| Shared integration | Generated OpenAPI and app.ts are shared boundaries. Avoid editing Access code. If both workers change generated OpenAPI, manager sequences the merges and regenerates with documented `yarn gen`; do not discard the other worker's API change. Tell the manager before altering QA registry, OAuth scopes or Content migrations. |
| Checks | Tests with implementation: native Go oracle, unit counterexamples, real private PostgreSQL/Main API persistence/denial/replay, relevant static checks. Use documented root commands and affected checks; manager repeats them after merge. |
| Estimate | Initial 60–90 minutes to an integrated result; re-estimate if native local-replacement rules expose a larger profile boundary. |
| Handoff | Commit on a local worktree branch, no remote push. Send the manager commit, paths/IDs, exact commands/results, semantic limitations and shared generated-file note, then finish. |

Local source bytes are an exact supplied snapshot; an API server path must not
become an implicit authority to read a caller-chosen directory.
