# G-022 — Realm experience Rating observation identity

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 38 |
| State | Integrated and affected-verified on `main` as `9f2a6d4`; RATE02 declared a complete-case candidate pending final recorded backend QA. Worker task `01a0dcdd-43d7-7a30-902e-e03740f19fc7` finished. |
| Base | Clean `main` at `001b514`, worktree `/home/edge/.codex/worktrees/e363/rezics-next`, independent of G-020 Access/Realm moderation |
| Deliverable | Add a separately versioned experience-cadence RatingContext and Observation API. One intentional, bounded occasion creates a new private-principal/Context/MainVersion Observation; correction and withdrawal revise that same Observation under an exact expected head. An idempotent retry of the original occasion or revision cannot create another slot. The caller may distinguish intentional occasions but cannot forge the private counting identity or select another principal/persona. |
| Acceptance | Seek a complete RATE02 candidate across standing, daily and experience observations on merged source. Preserve RATE03 daily calendar and RATE04 withdrawal behavior and receipts. RATE01 aggregation policies and broader temporal/event-time behavior remain separate. Declare RATE02 complete only if the whole [acceptance row](../../testing/ratings-and-event-time.md) passes through real owners and the registered matrix; final recorded backend QA decides qualification. |
| Dependencies | Integrated G-015 daily Rating owner, existing standing and daily revision/manifest/shape/recovery paths, Access `rating.observation.set`, Account private principal, [Rating contract](../../contracts/ratings.md#context-observation-and-revision). Apply `api-ui-design` and `research-and-validation` to occasion identity and revision semantics before coding. Design the versioned owner schema, immutable receipt/manifest, indexed slot lookup and bounded cost first. Avoid treating a new idempotency key by itself as a new experience. |
| Owned paths | Rating context/observation modules and versioned model definitions, Main Rating route/schema hunks, native Jena command if needed, rating contract/testing docs, affected real API/model/recovery fixtures. Do not change G-020 Access/Realm/Work moderation owners. Manager sequences shared `app.ts`, generated artifacts and any recovery integration. |
| Checks | Write tests with implementation. Standing correction, daily same-day correction and one experience correction retain each Observation identity and produce a new immutable revision; a deliberately new experience occasion creates another Observation without changing existing ones. Concurrent first submission and exact retry converge, while changed payload/reused key or occasion has explicit conflict behavior. Persona switching cannot multiply one occasion; another private principal is distinct. Withdrawal cannot resurrect an older opinion; stale head, revoked authority, inactive Account and unavailable owner deny new effects. Verify original evaluation/submission versus revision timestamps, exact private reads and public-principal minimization. Recover all three profiles across isolated graph loss and prove old standing/daily receipts remain readable. Bound indexed reads, graph calls/bytes and writes under growing unrelated observations. Run documented selected `yarn test` paths, relevant model/native checks, `yarn gen`, `yarn check:backend`, `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 150–240 minutes after occasion/schema design; report any unreconciled same-occasion versus new-occasion ambiguity early. |
| Handoff | Worker commit `9f855cd` merged as `9f2a6d4`. Merged source passed 46 selected units, backend static checks, native model `20260926t091143-ca7abe`, three-cadence real API/graph recovery `20260926t091407-a78047`, and standing withdrawal plus G-020 moderation regression `20260926t091452-2fb9a7`. G-022's occasion stays private and its public handle opaque; RATE02's full row is a candidate for final recorded backend qualification. RATE01 aggregation and deployment capacity remain retained. |

This is independent of G-020's organization moderation path. Shared Main routes,
generated OpenAPI and retained recovery fixtures are integrated serially by the
manager after both handoffs. No frontend or browser implementation is in scope.
