# G-019 — npm locked nested and peer-host topology

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 30 |
| State | Worker implemented and handed off commit `845895d`; manager integration awaits G-018 merged checks |
| Base | Clean `main` at `c2a2f55`, independent worktree `/home/edge/.codex/worktrees/9ca1/rezics-next` |
| Deliverable | Add a separately versioned, bounded npm lockfile-v3 import/profile to the private package owner API. Retain canonical caller-supplied `package.json` and `package-lock.json` bytes, digests, npm version/policy, package path, source and integrity identity. Represent two incompatible nested versions as distinct installed instances and bind every required peer to the actual ancestor host instance. Return an immutable exact receipt and private read/replay through Main. This profile validates an admitted installed topology; it must not claim to solve arbitrary version ranges or execute an installation. |
| Acceptance | Partial PKG03/PKG12/PKG13 evidence for lockfile-v3 nested and peer-host interpretation. Optional/platform/alias/workspace overrides, live registry resolution, artifact download, full npm/pnpm/Yarn solver behavior and installation remain retained later work. Do not close PKG03 from a lock parser or a source-only graph. |
| Dependencies | Merged package owner/API and signed recovery coverage through G-016. G-018 owns Content migration 019; reserve 020 for npm and integrate after 019, although their implementation worktrees are independent. Compare the exact admitted profile with official npm package/lock semantics and the pinned native npm CLI before fixing the wire contract. First update [toolchain](../../development/toolchain.md) to admit the exact native npm version and a root oracle command before invoking it; use no unlisted tool. Apply `research-and-validation` and `api-ui-design`. Design the durable owner schema, identity/uniqueness, response and recovery cut before routes. Official references: [package-lock.json](https://docs.npmjs.com/cli/v11/configuring-npm/package-lock-json), [package.json](https://docs.npmjs.com/cli/v11/configuring-npm/package-json) and [npm install](https://docs.npmjs.com/cli/v11/commands/npm-install). |
| Owned paths | Package npm module, package-owner migration, bounded package route/schema hunks in Main `app.ts`, generated OpenAPI, a fixed offline native npm oracle through a documented root command, unit and real Account/Access/Main/PostgreSQL API tests, isolated receipt recovery and package contract/testing/toolchain docs. Avoid Access and Source/Content owner changes; manager sequences shared route/generated/recovery files. |
| Checks | Write tests with implementation. Native npm and the admitted profile agree on a fixed lockfile with two incompatible versions of one name nested under different hosts, one required peer satisfied by the correct ancestor, and an incompatible or absent peer rejected without falling back to a same-name instance elsewhere. Compare paths, versions, peer hosts and `integrity`/resolved source provenance, not package counts alone. Reject malformed or mismatched root/lock, duplicate paths, missing referenced nodes, unsupported link/workspace/alias/platform clauses and bounded-budget exhaustion with distinct truthful outcomes. A changed byte or policy under one idempotency key conflicts; exact replay and private read preserve the original bytes/receipt; scope denial and inactive principal prevent new effects. Recover v1/v2/v3 Cargo and existing Go receipts alongside the npm receipt after a physical owner cut. Bound loaded nodes, edges, bytes and selected rows as unrelated locked history grows. Run documented affected `yarn test` paths, the new pinned root npm oracle command, `yarn gen`, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 150–240 minutes after offline native fixture/profile and owner schema are fixed; report an npm lock host ambiguity before widening the supported grammar. |
| Handoff | Worker committed `845895d` and finished. Eight-case pinned native npm oracle, eight npm unit tests, existing Cargo units, generation, backend static and docs passed. The worktree's real API attempt `20260926t074528-c08118` stopped at duplicate migration 017 before npm behavior; the worker corrected npm to 020 and left API plus physical recovery for manager verification after Source 019 merges. No remote push or full backend QA. |

The bounded profile is useful only when it preserves the actual peer environment.
An unsupported lock construct must not be silently discarded or presented as an
installable plan.
