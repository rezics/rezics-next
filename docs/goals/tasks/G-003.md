# G-003 — Membership-dependent group and role authority

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 2 |
| State | Ready after G-002 integration; manager records running/verification in the [plan](../../plan/README.md) |
| Base | `dc43d9454e055ab5fb244313abeca026f90b91ae` or later clean `main` after the management status commit; start in a fresh isolated worktree |
| Deliverable | Extend the G-002 membership episode dependency beyond direct `permission_grant` to existing `group_permission_grant` and `role_binding` paths. Bind a dependent grant/binding to the exact qualified membership ID and generation at creation, reject stale/inactive dependencies in the owning transaction and at command admission/claim, and make leave atomically revoke or permanently invalidate all old-episode dependent authority. Rejoin must never revive it; independent grants and another kind's membership survive. Keep bounded SQL calls and a fixed cleanup cap with typed unavailable instead of partial commit. |
| Acceptance | IAM06 dependent-authority assertions for group and role paths, plus applicable IAM33 stale-proof checks. This task remains a partial contribution to IAM06 until recipient consent verification and principal-member admission are covered. Do not mark either ID complete from only this slice. |
| Dependencies | G-002 migration `020_membership_lifecycle.sql`, Access membership owner, group and role owner modules, selected admission proof, Account OAuth boundary. Preserve all G-002 direct-grant behavior and the historical migration fixtures. |
| Owned paths | New Access migration `021_membership_dependent_authority.sql`, `services/main/src/modules/access/groups.ts`, `roles.ts`, membership owner and admission proof where required, relevant Access/Work recovery coverage, `services/main/src/app.ts` group/role request schemas/routes, generated OpenAPI, focused `tests/qa/` and owning contract/tests. Do not modify package checksum files. |
| Shared integration | This task owns new Access migration `021` and the affected app.ts sections until handoff. Announce any Account OAuth scope, QA registry or generated API overlap before changing it. Manager integrates and runs merged checks. |
| Checks | Write tests with implementation, including real Account/Access/Main/PostgreSQL positive, stale, leave/rejoin, concurrent and denied cases. Use documented explicit `yarn test` paths and selected backend static checks for blocking diagnosis. Include an existing direct-grant regression and old-migration fixture. |
| Estimate | Initial 60–90 minutes to an integrated result; revise if the group/role proof contract exposes a wider dependency. |
| Handoff | Commit on a local worktree branch, no remote push. Send the manager commit, affected paths/IDs, exact commands/results, any uncovered membership path and next action, then finish. |

Do not substitute a row update for admission revalidation. An already prepared
proof bound to an old membership episode must fail at claim, and a database
constraint or trigger must prevent reactivating an old dependent authority row.
