# G-006 — Private-principal membership admission

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 5 |
| State | Prepared; dispatch after G-004's consent schema and merged checks are stable |
| Base | The verified `main` commit after G-004 integration; do not branch from its worktree |
| Deliverable | Admit an authenticated private principal directly to one Org roster or Realm participation policy without manufacturing a public Agent or exposing the account-to-membership mapping. Preserve independent policy/terms, recipient consent, bans, monotonic leave/rejoin episode, immutable history, manager authority and private recipient read. Design the Access owner schema and exact recipient identity first, then implement a bounded command/API profile. A principal deactivation or Account deletion fence must make membership-derived authority unusable. |
| Acceptance | IAM06 private-principal membership and independent Org/Realm admission clauses; IAM10 where the real Account/Access boundary applies. This task must not claim all IAM06 until group/role/Realm and recovery obligations are independently qualified. |
| Dependencies | Integrated G-002/G-003 episode and dependent-authority rules and G-004 recipient consent artifact. Use `api-ui-design` for the private API contract and preserve Account as principal identity owner, Access as membership/authority owner and Main as API composition. |
| Owned paths | A new Access migration after G-004, principal membership/consent owner modules, Main membership and recipient route sections, generated OpenAPI, real Account/Access/Main/PostgreSQL integration, recovery digest and owning contracts. Coordinate shared `app.ts`/OpenAPI with the manager before editing. No package or source code. |
| Checks | Test with implementation: separate manager/recipient OAuth, private read isolation, Org versus Realm policy separation, consent mismatch/replay, leave/rejoin generation, independent ban, principal deactivation, deleted-account fence, dependent authority denial, idempotent/stale/conflicting commands and isolated restore. Use documented affected `yarn test` paths and `yarn check:backend`; manager repeats on merged source. |
| Estimate | Initial 90–120 minutes after schema review; report any broader owner dependency before changing scope. |
| Handoff | Commit on a fresh isolated worktree branch. Send commit, paths/IDs, commands/results, blockers and exact remaining IAM06 limitations, then finish. |

Keep a private principal's membership identity distinct from a public Agent. A
manager's ability to change a roster does not let it mint the recipient's consent.
