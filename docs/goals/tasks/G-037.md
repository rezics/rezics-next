# G-037 — invalid OIDC authorization state at the Account boundary

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 91; brief revision 1 |
| State | Ready for one pinned Sol/xhigh Account worker in an independent worktree after G-036 handoff. IAM02 remains partial. |
| Deliverable | Qualify the IAM02 backend operation through real Account HTTP/OIDC requests: invalid issuer, audience, redirect and CSRF/state replay must reject without changing the account, consent, token or session authority state. Keep IAM01's valid two-product flow and IAM09's revocation contract intact. |
| Owner audit | Identify Better Auth 1.7.5's exact authorization/token/redirect boundary, stored state and caller contracts before editing. Document which invalid cases are enforceable at Account's API and which require the registered client's callback proof. Do not infer success from a mocked frontend callback. Use the existing Account/OIDC schema; add owner state only for a demonstrated gap. |
| Real template gate | First run one real valid authorization-code/PKCE HTTP journey and one rejected invalid request against isolated Account storage, then extend the matrix. No browser or frontend acceptance. The fixture must show before/after durable Account state and token introspection or session read, not only an error status. |
| Acceptance | Invalid issuer/audience or swapped client, redirect mismatch/open redirect, missing/wrong/replayed state or CSRF, replayed code and invalid PKCE verifier fail closed at the correct boundary. A failed attempt leaves the valid principal's session, consent, grant and refresh authority unchanged. A valid registered-client flow still succeeds; avoid leaking secrets in test artifacts. State explicitly which concerns are client-owned and cannot be certified through Account HTTP alone. Declare IAM02 complete only if all four named hazards and no-state-change behavior are proved with real boundaries. |
| Owned paths | Account auth/configuration modules and isolated Account API/integration tests. Narrow Main tests only for an actual Account consumer boundary. The manager owns shared QA scheduling, complete-case coverage mapping, plan and merge. Avoid Access migrations, Work, Search, Rating and external Resource/Statement docs. |
| Checks | Tests with implementation. Use documented affected `yarn test` paths and `yarn check:backend`; coordinate the shared QA stack before real integration, no full QA or public-corpus seeding. Routine preparation including startup/readiness is limited to 600 seconds. |
| Model and isolation | Pinned `gpt-6-sol`/`xhigh` due OIDC security and multi-owner replay semantics. Astra is forbidden. No recursive delegation. Work from a fresh brief in `.temp/worktrees/g037-iam02` and commit one coherent result. |
| Estimate | 60–120 minutes after the real template gate; report an issuer/state boundary blocker promptly. The original 13:57:20 UTC target is forecast to miss. |
| Handoff | Finish after one committed result. Return the commit, exact test/artifact IDs, case claim or blocker, unchanged-authority proof and integration action. No remote push. |

IAM02 is independent of G-036's role SQL and the Resource/Statement docs work.
The manager serializes any Main route and the shared QA environment. All 276
backend acceptance cases remain retained.
