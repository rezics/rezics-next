# G-018 — Source-qualified author occurrence adoption

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 29 |
| State | Running in task `01a0dc8f-baf0-7b11-9328-a691aa18d3d7` |
| Base | Clean `main` at `0e98abd`, independent worktree `/home/edge/.codex/worktrees/a29a/rezics-next` |
| Deliverable | Add one bounded native Work author-credit relation profile and an explicit API operation that adopts one source-qualified author occurrence from a verified complete Open Library Work conversion. The retained source key, observation-qualified occurrence, ordinal, role, conversion/proposal and correspondence decision remain distinct from native credit identity. The operation must never infer that a source author key is an Agent, merge repeated children by text/key alone, or rewrite an existing credit on refresh. |
| Acceptance | Partial LIVE04/MODEL05/MODEL06 evidence for one author-credit occurrence and its exact native history. Other child families, split/merge arbitration, broad field adoption, identity reconciliation and complete source-control/protection policy remain retained later work. Do not close LIVE04 or a model ID from source-only correspondence or a graph shape alone. |
| Dependencies | Merged G-014 Source support and existing complete Open Library conversion, drift and explicit source-only correspondence routes; [source child contract](../../contracts/source-lifecycle.md#child-correspondence-and-structure), [semantic relation contract](../../contracts/semantic-model.md#seven-contracts-and-one-model-representation), and [source tests](../../testing/source-conformance.md#field-and-workflow-coverage). Apply `api-ui-design` and `research-and-validation` to the native relation/operation before coding. Design the authoritative owner schema, unique occurrence identity and recovery cut first. Use existing Work edit admission without changing Access owner modules. |
| Owned paths | Source/Content owner migration and author-credit module, narrow native graph/model profile and Main route/schema hunks, generated artifacts, real Account/Access/Main/Content/Jena API fixture, isolated recovery and bounded growth tests, source/model contract and testing docs. Avoid G-017 Access owner and G-016 package paths; manager sequences shared `app.ts`, generated registries and migrations. |
| Checks | Write tests with implementation. Two repeated source author keys remain two occurrence identities; reorder preserves explicit correspondence without silently changing a native credit, while reuse or ambiguous pairing conflicts. Wrong Work/Edition grain, incomplete/unmapped author list, stale conversion/proposal, wrong principal, absent `work.edit`, changed Work head and unrelated SourceRecord deny mutation. Exact authorized retry returns one immutable receipt; changed intent conflicts. A human/native edit and another source support survive source withdrawal; a missing source child never deletes native identity. Prove the accepted credit, source proof and graph effect across owner recovery; bound selected rows, graph calls, bytes and writes for one occurrence independent of history length. Run documented affected `yarn test` paths, `yarn gen`, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 150–210 minutes after relation/owner schema design; report a missing native Work relation boundary before widening scope. |
| Handoff | Commit in the isolated worktree, no remote push. Send manager commit, exact checks/run IDs, remaining LIVE/MODEL boundaries and generated-file note, then finish. |

This slice adopts one attributable credit occurrence. It does not make provider
child identity authoritative for a native person/Agent or infer deletion from an
absent source list.
