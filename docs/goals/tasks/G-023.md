# G-023 — npm alias and workspace lock identity

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 40 |
| State | Running in independent worktree task `01a0dce4-6caf-7960-916a-5f761b191811` |
| Base | Clean `main` at `fc23357`, worktree `/home/edge/.codex/worktrees/070b/rezics-next`, independent of G-022 Rating/model ownership |
| Deliverable | Add a separately versioned bounded npm lockfile-v3 profile for exact caller-supplied alias and workspace-link topology. Preserve the declared dependency name, package identity, literal artifact source/SRI, installed path, workspace target and peer-host environment as distinct facts. Validate only lock meaning actually supported by pinned npm 11.19.1's offline virtual tree; no registry solving, artifact download or physical installation claim. Keep v1 and v2 request bytes, receipt shapes and replay frozen. |
| Acceptance | Partial PKG04/PKG12/PKG13 evidence with native counterexamples. A new alias or workspace profile must not silently reinterpret an old v1/v2 receipt. Override and engine policy, pnpm/Yarn, live registry selection, artifact integrity and installation remain retained. Do not declare PKG04 complete from virtual-tree tests alone. |
| Dependencies | Integrated G-019 npm nested/peer v1 and G-021 optional/platform v2, immutable `pkg.npm_resolution` owner and signed Content recovery v4, [package profile contract](../../contracts/package-profiles.md#npm-pnpm-and-yarn), pinned npm 11.19.1. Apply `research-and-validation` and `api-ui-design` before fixing the wire profile. Design alias/workspace identity and receipt/recovery compatibility first; test native behavior before widening grammar. If an oracle command or tool is needed, update [toolchain](../../development/toolchain.md) before use. |
| Owned paths | npm parser/resolution/schema/profile, fixed offline native oracle/fixtures, narrow Main package route/client hunks, selected unit/real API/physical recovery tests, package contract/testing docs. Avoid G-022 Rating/model owner files. Manager sequences shared `app.ts`, generated OpenAPI and recovery fixtures after handoff. |
| Checks | Write tests with implementation. Compare at least a registry alias whose declared name differs from the package identity, two alias slots to one artifact, a workspace link with internal dependency and peer host, a workspace name/path mismatch, an alias with incompatible peer, and missing or malformed link/source/SRI. Compare exact native paths, versions, declared and resolved names, link target, source/SRI and peer hosts or explicit unsupported/incomplete outcomes. Unknown source grammar and override/engine clauses must have truthful distinct outcomes; no path/name collapse. Prove same-key replay, changed bytes/key conflict, inactive-principal denial and v1/v2 immutable read compatibility through real API. Recover old and new npm receipts with Go/Cargo after an isolated physical cut. Bound bytes, nodes, edges, path depth and ancestor lookups; measure indexed receipt reads under unrelated history. Run documented affected `yarn test` paths, `yarn package:npm-oracle`, `yarn gen`, `yarn check:backend`, `yarn docs:check`; manager repeats selected merged checks. |
| Estimate | Initial 180–240 minutes after pinned native counterexamples and owner schema. Report any workspace/alias ambiguity before widening the grammar. |
| Handoff | Commit one coherent branch result; return commit hash, owned paths, exact commands/artifacts, unresolved semantics and required manager integration, then finish. No remote push or full backend QA. |

The package boundary is independent of G-022's Rating work. Shared Main routes,
generated contracts and owner-cut verification are integrated serially by the
manager. All remaining package acceptance assertions stay in the Goal.
