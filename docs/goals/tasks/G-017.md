# G-017 — Explicit managed-organization authority

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 28 |
| State | Running in task `01a0dc7f-9f18-7443-a567-4d5bd4e15dc3` |
| Base | Clean `main` at `cfcb5a1`, independent worktree `/home/edge/.codex/worktrees/9fdf/rezics-next` |
| Deliverable | Add an Access-owned, expressly granted management relationship from one admitted organization to one admitted Realm or parent authority. Its immutable grant states named administrative actions, resource scope, validity, delegation ceiling and generation. A separate, current organization mandate authorizes issuing or revoking it; the recipient's current representation and matching grant are needed to exercise it. A structural Org-to-Realm participation tuple, move, membership or public description never creates or widens management authority. |
| Acceptance | Partial IAM24/IAM23/IAM06 evidence for the managed-mode authority boundary and one real protected organization operation. Founding grants, general control/recovery, voting, publication moderation, paid benefits, moves and complete Realm quota/review remain separately retained unless this slice actually implements and verifies them. Do not close a retained ID from schema or decision-only tests. |
| Dependencies | Merged G-012 independent participation, [Realm participation](../../contracts/realm-participation.md#organizational-authority-boundaries), [identity and access](../../contracts/identity-and-access.md#roles-bindings-and-grantability), and the final G-014 Access admission helper review. Apply `api-ui-design` and `research-and-validation` to the action/resource/ceiling vocabulary and API before coding. Design owner schema and exact dependent revocation behavior first. Preserve separate Account, Access and Content authority boundaries. |
| Owned paths | Access migration after 025, Access owner modules and one named organization operation as a real consumer, bounded route/schema hunks in Main `app.ts`, generated OpenAPI, real Account/Main/Access/PostgreSQL integration and isolated Access recovery tests, Realm/identity contracts and testing docs. Do not edit Source, Rating/model or package owners; manager sequences shared generated files. |
| Checks | Write tests with implementation. Show that independent participation alone, membership, descriptive links and a second Realm cannot exercise or expand management. An explicit organization-issued grant permits only its named action within its time, scope and ceiling; unauthorized issuance, stale issuer/recipient authority, a changed grant generation, a narrower ceiling, revocation, expired representation and rejoin deny new effects. Exercise exact retry and changed-key conflict, concurrent issue/revoke, Account deactivation, and WAL recovery of the grant and protected operation. Bound indexed lookup, selected rows, lock wait and write amplification; no roster expansion per command. Run documented affected `yarn test` paths, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 120–180 minutes after the owner/action vocabulary and protected consumer are fixed; report a missing issuer mandate or protected-operation boundary before widening scope. |
| Handoff | Commit in the isolated worktree, no remote push. Send manager commit, exact checks/run IDs, remaining IAM boundaries and generated-file note, then finish. |

This grant represents an explicit managed mode. It cannot be inferred from the
independent participation implemented in G-012, and it must have a real consumer
before any managed-operation assertion is counted as verified.
