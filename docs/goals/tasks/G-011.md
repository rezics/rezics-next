# G-011 — Cargo native `links` conflict proof

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 10 |
| State | Prepared for an independent package slot after G-008 integration and one active handoff |
| Base | Clean verified `main` at dispatch, in a fresh worktree |
| Deliverable | Extend the exact Cargo registry snapshot with a separately versioned bounded profile that admits native `links` metadata. Two selected package identities claiming the same native library name must produce a truthful, deterministic unsatisfiable outcome with the conflicting source/name/version identities. A nonconflicting graph preserves G-008's lock selection, host/target feature graph, private immutable resolution and idempotent replay. Keep the v1 profile and stored v1 outcomes unchanged. |
| Acceptance | Partial PKG02/PKG12/PKG13 evidence for native `links` conflict versus solvable graph; no full ID is closed. Yanked fresh/locked eligibility, general semver/backtracking, other Cargo constraints and installation remain separate requirements. |
| Dependencies | Merged G-008 Cargo owner/schema, exact index bytes and pinned Cargo 1.98.1 local-registry oracle. Use `research-and-validation` and `api-ui-design` to choose the versioned request/outcome contract, with the official Cargo resolver and index rules plus a native differential fixture. Design the conflict receipt and durable outcome before API edits. |
| Owned paths | Cargo package owner/module and oracle, package route/schema hunks of Main `app.ts`, generated OpenAPI, package unit and real Account/Access/PostgreSQL API tests, package contracts/testing docs. Avoid Access and Source code, migrations and route hunks. Coordinate generated OpenAPI and shared `app.ts` with manager. |
| Checks | With tests: same `links` from incompatible exact selected versions is unsatisfiable in native Cargo and the new profile; different `links` or one selected owner resolves; unselected releases cannot create conflict; source/name/version and role identity remain exact; v1 still reports `links` unsupported and replays old rows unchanged; malformed/incomplete/budget outcomes stay distinct; private read, replay, denial and restore survive. Run documented affected `yarn test` paths, `yarn package:cargo-oracle`, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 75–120 minutes; report a native semantic or fixture blocker before widening scope. |
| Handoff | Commit on isolated worktree, no remote push. Send manager commit, paths/IDs, checks and evidence, remaining limits and generated-file note, then finish. |

This slice proves only the exact selected native-library conflict. It must not
label incomplete source data, unsupported syntax or a search-budget stop as
unsatisfiable, and must not infer yanked-lock behavior from a fresh solve.
