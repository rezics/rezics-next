# G-016 — Go pruned graph with main replace and exclude

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 26 |
| State | Integrated and affected-verified on `f54c79f`; retained PKG IDs partial |
| Base | Clean `main` at `68cf3e7`, independent worktree `/home/edge/.codex/worktrees/a4c5/rezics-next` |
| Deliverable | Add a separately versioned bounded captured Go 1.17+ MVS profile that applies main-module exact/path-wide remote `replace` and exact-version `exclude` while preserving graph pruning and lazy manifest loading. Keep original module identity, selected version, replacement source identity and exact capture provenance distinct. Existing Go 1.16 directive and 1.17+ no-directive request/receipt behavior must remain unchanged. |
| Acceptance | Partial PKG05/PKG12/PKG13 evidence for combined pruning and main directives; live provider selection, workspace/local replacement combinations, checksums of all captures, lock/install and broad release-version sets remain separate. Do not close a retained ID on this slice. |
| Dependencies | Merged G-005/G-007 Go owners and captured private Go source, exact v2 main-directive behavior, pinned Go 1.27.1 native oracle. Apply `research-and-validation` and `api-ui-design` to the versioned request/outcome before coding. Read the official Go module graph pruning and `replace`/`exclude` semantics, then use native local-file proxy cases that force a distinction between pruned and unpruned expansion. Design immutable request/source/digest identity before API edits. |
| Owned paths | Go package parser/MVS/capture resolution and native oracle; package Go route/schema hunks in Main `app.ts`, generated OpenAPI; package unit and real Account/Access/Main/PostgreSQL API tests; package contract/testing docs. Avoid Source, Rating/model and Access owner edits; manager sequences shared generated files. |
| Checks | Write tests with implementation. Pinned Go and REZICS agree on selected build list and replacement source for exact and path-wide remote rules, exclude of a required version, rule precedence, `/v2` path identity and a pruned branch whose unneeded `.mod` bytes are withheld. An excluded/replaced path cannot silently use a wrong captured manifest or fabricate checksum provenance. Distinguish missing required capture, unsupported directive, malformed/duplicate rule and budget exhaustion; selected but unexpanded modules retain honest source availability. Verify private create/read, idempotent replay, changed-key conflict, inactive principal and physical recovery of old/new receipts. Run documented affected `yarn test` paths, `yarn package:go-oracle`, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 120–180 minutes after native differential profile design; report an unresolved native semantic or source-identity conflict before widening scope. |
| Handoff | Worker committed `4410466` and finished. Manager merged `f54c79f`, repaired current Jena readiness at `6a98d53`, and recorded merged API `20260926t071418-ef61d7` plus six-receipt restore `20260926t071823-bba00f`. No remote push or full backend QA. |

This profile resolves a captured build list; it does not fetch provider data at
resolution time, verify all module artifacts, install packages or treat a Go
replacement as a new original module coordinate.
