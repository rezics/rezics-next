# G-032 — IAM03 many-to-many acting-context qualification

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 64 |
| State | Manager-owned integrated and affected-verified batch on `742601e`; IAM03 complete-case candidate pending recorded backend QA |
| Base | G-031's verified two-product Account/Access/Main acting-context fixture on `95f95b5`; independent of G-028 Work title and G-029 Rating policy modules |
| Deliverable | Qualify IAM03's many-to-many principal/Agent relationship through real Account OAuth, Access owner and Main APIs. One principal selects two different admitted Agents; two principals independently select the same Agent. Neither response exposes the private Account IDs or Access principal IDs. |
| Acceptance | The owner fixture proves the two mappings and denies unrelated Agent selection. Explicit privacy checks cover the second principal's discovery and concurrent shared-Agent selection responses. The merged selected test and backend static checks passed, making IAM03 a backend-only complete-case candidate, not a claim for IAM04's broader pooling or any public controller directory. |
| Owned paths | `services/main/tests/acting-context.integration.test.ts`, `scripts/qa/coverage.ts`, IAM testing notes and the plan; no worker worktree edits. |
| Checks | Selected real Account/Access/Main API `20260926t110324-ddd7ee` passed; 11 QA coverage units, `yarn check:backend` (including docs) passed. The selected run reported partial passes because it cannot certify the full inventory. |
| Handoff | Manager commits the coherent test/coverage/docs batch on `main` with exact evidence; final recorded `yarn qa --backend --record` remains mandatory. |

General representation issuance, eligible member sets, grant pooling and
host-capacity assertions remain in their own retained acceptance cases.
