# G-001 — Exact Go pseudo-version checksum qualification

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 1 |
| State | Ready at handover; manager records running/verification in the [plan](../../plan/README.md) |
| Base | `f2c8aa1` on `main`; start in a fresh isolated worktree |
| Deliverable | Complete the pending B75 slice: select an exact canonical pseudo-version from the official pinned Go toolchain's retained module manifest, capture that exact `.info`/`.mod` from the fixed proxy, compare Go's verified checksum with REZICS's exact bytes, and verify signed `sum.golang.org` inclusion plus consistency against the durable checkpoint. Include refusal cases for mismatched version, checksum, record or tree and preserve bounded provider calls. |
| Acceptance | Partial PKG05, PKG14, PKG20 only. The task closes its stated pseudo-version provenance assertions; it cannot claim these full IDs while the broader ecosystem, lock and live-refresh cases remain open. |
| Dependencies | B63–B74 signed note, inclusion/consistency, trust owner and exact pseudo capture already on `main`. No new tool, Go version or external service without first updating the toolchain lock. |
| Owned paths | `scripts/package/go-checksum-oracle.ts`, package checksum/capture tests under `tests/qa/`, package modules under `services/main/src/modules/package/` if needed, `docs/testing/packages.md`. Avoid unrelated Access and membership modules. |
| Shared integration | Tell the manager before editing `services/main/src/app.ts`, package route registration, generated OpenAPI or QA registry; the manager owns sequencing of shared registration and generated files. |
| Checks | Add tests with implementation. Use documented `yarn test <paths>`, `yarn package:go-checksum-oracle` and relevant backend static checks for a blocking diagnosis. Manager runs affected merged checks; final full backend QA remains separate. |
| Estimate | Initial 60–90 minutes to an integrated result; revise from measured handoff and QA. |
| Handoff | Commit on the worktree branch. Return commit, affected paths/IDs, exact commands/results, source/provenance fixture, blockers and remaining PKG05/14/20 limitations. Then finish. |

No success claim may depend only on a checksum string match; the signed record,
tree consistency and exact capture identity must be tied to the same version.
