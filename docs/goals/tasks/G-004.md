# G-004 — Recipient-verified membership consent

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 2 |
| State | Planned; dispatch after G-003 releases the shared Access and app.ts boundary |
| Base | The verified `main` commit after G-003 integration; do not branch from G-003's unverified worktree |
| Deliverable | Replace G-002's caller-supplied `consentReference` with an immutable, recipient-verified membership consent artifact. An authenticated principal with a qualified mandate for the member Agent records consent for one Org/Realm owner, member, policy/terms revision and expected next admission generation. The manager's join operation verifies that exact artifact, current principal/Agent state and bounded expiry/revocation conditions before committing; leave does not erase evidence, and rejoin needs a newly admitted consent. Reject forged, cross-owner/kind/member, stale, expired, revoked and replayed-for-a-different-episode references. |
| Acceptance | IAM06 consent/terms and rejoin authority clauses, with IAM09/IAM10 only where their own complete cases apply. IAM06 stays partial until principal-member and any other retained membership paths are qualified. |
| Dependencies | G-002 Agent-member owner and G-003 group/role episode proof; existing Better Auth OAuth, Access representation, policy revision and recovery fence. Use `api-ui-design` skill for the recipient and manager API contract. Preserve the Account/Access owner split. |
| Owned paths | New Access migration (reserve `022` after G-003), consent owner module, membership join verification, Account scope configuration if necessary, Main route/schema and generated OpenAPI, real OAuth/API/integration and recovery tests, owning contracts. Manager coordinates any shared route or scope change before implementation. |
| Checks | Tests with implementation: recipient versus manager OAuth tokens, authorization and mandate, exact consent binding, concurrent join, leave/rejoin, replay, denial/unavailable and isolated restore. Use documented affected `yarn test` paths and backend static checks; manager repeats on merged source. |
| Estimate | Initial 60–90 minutes to an integrated result; re-estimate after checking the Account scope and mandate interfaces. |
| Handoff | Commit on an isolated worktree branch. Send the manager commit, paths/IDs, exact commands/results, blockers and remaining IAM06 limitations, then finish. |

The consent artifact must be issued by the qualified recipient path. A manager
cannot make membership valid by inventing a nonempty reference string.
