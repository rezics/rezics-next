# G-034 — eligible Org member-set grant selector

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 72 |
| State | Running with explicitly pinned Sol/xhigh worker `/root/g034_eligible_sol` in independent `.temp/worktrees/g034-eligible-selector` at `81d598b`. IAM25 remains partial. |
| Base | Merged G-030 and G-033 on clean `main`; Access migration 031, exact represented B-roster operation and private Org A membership episodes are available. Use a new `.temp/worktrees/g034-eligible-selector` branch and reserve Access migration 032 or later. |
| Deliverable | Add one bounded, explicit grant to A's eligible private-member set for `access.membership.manage.org` on B's exact Org roster scope. P may use that selector as P, without becoming A's representative. Keep G-030's P→A mandate plus B→A institutional grant path independent. Build an Access-owned versioned selector/grant episode, private proof and immutable effect/receipt, narrow real Main write/read routes and recovery coverage. |
| Schema gap | `access.permission_grant.recipient_subject` names an Agent, not a typed eligible set. `access.private_membership` already binds P to an exact Org A membership episode, and `access.org_roster_scope` binds B's roster, but no selector identity, B-issued set grant, eligibility witness or exact receipt exists. The G-030 write/read API supplies a real B-roster operation template, not this selector's authority. Design the missing owner tables, unique keys, indexes, immutability and recovery digest before routes. Verify one real write/read API template against Account, Access and Main before cloning repetitive paths. |
| Acceptance | IAM25 must deny P when P is only A's member, editor or administrator and A has B authority but P has neither a representation mandate nor a selected eligible-set grant. A separately issued B-to-A eligible-set grant may allow a current qualifying P as P under its declared selector. Check wrong A/B, out-of-set P, current membership generation, leave/rejoin, expired/revoked/stale grant, B policy and principal/Agent generation, target consent, same-key replay/conflict and concurrent effect. Public response must not expose P's private Account identity. Exact read, physical Access WAL restore and indexed cost under unrelated history are required. Keep IAM26 mapping and receipts unchanged. |
| Owned paths | Access migration 032+, selector/grant and roster operation owner, narrow Main Access routes/schema, IAM25 real Account/Access/Main API and Access WAL fixtures, identity contract/testing docs. Avoid package, Rating and Jena owners. The manager serially integrates `app.ts`, generated OpenAPI, QA coverage and shared recovery fixtures. |
| Decisions | Apply `api-ui-design` and `research-and-validation` before fixing the selector model. Admit one exact selector (current private Org A members) first; additional role or status predicates require explicit independent authority and tests. The selected proof cannot pool pieces of an A representation or personal B grant. Recipient set is not an acting identity. If the existing B-roster operation cannot carry P's direct selected proof without changing its meaning, add a separate narrow operation with its own receipt profile. |
| Checks | Write tests with implementation. Run documented affected `yarn test` API and Access WAL paths, `yarn gen`, `yarn check:backend`, `yarn docs:check`; give the manager the exact diagnostic artifacts. Do not run full QA or seed a public corpus. Keep routine test data preparation below 600 seconds. |
| Model and isolation | Sol/xhigh: this is an uncertain authority, transaction and recovery change, not bounded repetition. Independent worktree isolates shared Access routes and migration sequence. No Astra fallback. |
| Estimate | Initial 90–150 minutes after owner schema and real API proof stabilize; report a blocking authority mismatch before widening selector grammar. |
| Handoff | Commit one coherent worktree result and finish. Return commit, checks/artifacts, IAM25 claim, unsupported selectors, cost limits and next integration action. No remote push. |

The separate Access owner is justified by the distinct recipient-set identity and
eligibility proof; G-030 proves representation, not set membership selection.
Every other backend Goal assertion remains retained.
