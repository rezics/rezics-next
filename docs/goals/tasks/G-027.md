# G-027 — IAM06 complete-case reconciliation

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 50 |
| State | Integrated and affected-verified manager batch on `main`; IAM06 declared a complete-case candidate pending final recorded backend QA |
| Base | Clean `main` at `2b4ecc4`; G-025 Rating and G-026 npm worktrees remain independent |
| Deliverable | Reconcile the retained IAM06 leave/rejoin acceptance row with the already implemented real Org/Realm, private direct/group/role dependency and Access WAL cases. Correct stale test documentation and declare the exact four-case backend coverage set only if each named test exercises its owner boundary. |
| Acceptance | A leave/rejoin advances the admission generation without reviving dependent direct, private group or private role authority and without erasing a ban. Independent grants and unrelated Realm episodes survive. At 257 dependent rows, leave refuses the whole transition. A clean full backend run must execute all four declared cases before IAM06 can qualify. General role semantics, wider Realm publication and host capacity remain retained separately. |
| Owned paths | `scripts/qa/coverage.ts`, its unit test, IAM testing page and execution plan. Do not change active worker owner modules. |
| Checks | Run the four selected real-owner cases via documented `yarn test` paths, `yarn test tests/qa/unit/coverage.test.ts`, `yarn check:backend`, `yarn docs:check`, then commit. Existing merged G-024 Access WAL evidence may be reused if its source includes the IAM06 owners and the case title is unchanged. |
| Estimate | 10–20 minutes after reading the current test bodies. |
| Handoff | Merged real Org/Realm API `20260926t093654-8c1871`, private direct/group/role API `20260926t093821-c7cb5b`, Access WAL `20260926t093115-947f02`, coverage unit, backend static and documentation checks passed. Manager commits the declaration on `main`; no worker or remote push. |

This is a qualification mapping over existing behavior. It does not change an
owner implementation or waive any other backend Goal case.
