# G-031 — two-client acting-context isolation

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 60 |
| State | Manager batch implemented and affected-verified on `main`; no worker claim |
| Base | Verified `main` at `4801a1a`; independent of active Work/Source and Rating owner changes |
| Deliverable | Reconcile IAM01's retained backend obligation using real Account OAuth clients, Access private principals and Main acting-context requests. One authenticated account uses two distinct native product client registrations with separate code/PKCE tokens. Concurrent product requests explicitly select different valid Agents and retain their own selected outcome despite a shared optional saved preference. A second account cannot use the first account's other Agent. |
| Acceptance | Declare an IAM01 complete-case candidate only if the real owner fixture proves client registration, issuer/audience and token verification, request-scoped Agent choice across two OAuth clients, denied cross-principal choice and no public Account-to-principal mapping. The frontend tab UI remains outside the backend Goal. IAM02 invalid issuer/redirect/CSRF and IAM03 multi-representative governance remain separately retained. |
| Owned paths | `services/main/tests/acting-context.integration.test.ts`, IAM testing/plan docs and narrow QA coverage declaration. No worker-owned modules, migrations, `app.ts` or generated artifacts. |
| Checks | Register two native product clients through Account, perform independent authorization-code/PKCE exchanges for one user, interleave Main discovery/check calls using their tokens, and assert two different selected Agents without cross-request substitution. Use the existing second user for forbidden selections. Preserve current preference replay/CAS and recovery assertions. Run `yarn test services/main/tests/acting-context.integration.test.ts`, `yarn test tests/qa/unit/coverage.test.ts`, `yarn check:backend` and `yarn docs:check` on merged `main`. |
| Estimate | 30–60 minutes, including the real isolated owner run and documentation. |
| Handoff | Real owner selection `20260926t102108-25c37d`, QA coverage unit, `yarn check:backend` and documentation checks passed. The existing discovery-cost assertion was reconciled with its fixed 15-query path after a selected failure at the stale 13-query bound. The IAM01 claim remains a candidate until the final clean `yarn qa --backend --record`. |

All other backend Goal assertions remain retained.
