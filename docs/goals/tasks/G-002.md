# G-002 — Membership leave/rejoin authority lifecycle

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 1 |
| State | Ready at handover; manager records running/verification in the [plan](../../plan/README.md) |
| Base | `f2c8aa1` on `main`; start in a fresh isolated worktree |
| Deliverable | Implement the planned `POST /v1/access/membership-changes` owner operation for Org operational membership and Realm participation. Persist current state, admission generation, consent/terms basis and history. Leave and rejoin must advance admission generation, invalidate dependent authority from earlier generations, preserve bans and keep Realm/Org admission policies separate. Expose typed denied, stale, retry and unavailable outcomes with idempotent receipts and bounded owner work. |
| Acceptance | IAM06's backend assertion: after leave/rejoin, admission uses the new generation and cannot revive an old dependent grant or erase a ban. Include real Account/Access/Main/PostgreSQL API tests for positive, denied, stale, concurrent and retry paths. Any other IDs exercised remain partial unless their complete assertions pass. |
| Dependencies | Existing Account bearer verification, Access principal/grant/representation owner, scope gate, recovery fence and receipt patterns. Resolve wire shape and policy details against `docs/contracts/identity-and-access.md` and `docs/contracts/realm-participation.md` before code; use the `api-ui-design` skill for the semantic API contract. |
| Owned paths | New Access migration and membership module, `services/main/src/app.ts` route/schema, relevant `tests/qa/integration/` and unit tests, `docs/contracts/identity-and-access.md`, `docs/testing/identity-and-access.md`, `docs/plan/backend-operations.md`. Do not edit package modules or checksum scripts. |
| Shared integration | This task owns the `app.ts` edit while G-001 runs. Coordinate migration number, OAuth scopes, generated OpenAPI and QA registry with the manager before changing these shared boundaries. |
| Checks | Add tests with implementation. Use documented `yarn test <paths>` and relevant backend static checks only for blocking diagnosis. Manager runs affected merged checks; final full backend QA remains separate. |
| Estimate | Initial 60–90 minutes to an integrated result; revise from measured handoff and QA. |
| Handoff | Commit on the worktree branch. Return commit, migration number, affected paths/IDs, exact commands/results, remaining policy questions and limitations. Then finish. |

Do not treat group membership, Realm participation, Org membership and Agent
control as interchangeable. Dependent grants must bind the admission generation
they require; a new membership episode never silently reactivates an old grant.
