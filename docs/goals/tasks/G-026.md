# G-026 — composed npm identity and target projection

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 46 |
| State | Running in independent worktree task `01a0dd0c-d46b-7482-9f86-7002aad7bf04` |
| Base | Verified `main` at `37fe902`, worktree `/home/edge/.codex/worktrees/7b8c/rezics-next`; independent of G-024 Access transfer and G-025 Rating aggregation |
| Deliverable | Add a separately versioned bounded npm lockfile-v3 profile that composes G-021 optional/platform target projection with G-023 exact alias/workspace identity. Preserve declared slot versus resolved package, workspace link versus target, peer host, literal source/SRI, optional ancestry, target compatibility and omission cause in one receipt. Keep v1/v2/v3 request, outcome, ID and replay bytes frozen. |
| Acceptance | Seek partial PKG04/PKG12/PKG13 evidence against pinned npm 11.19.1 and its bundled Arborist; do not claim complete PKG04 while override/engine, broad workspaces, other package managers, registry solving, artifact validation and installation remain unimplemented. No optional branch may erase a missing required dependency or a wrong alias/workspace identity. |
| Dependencies | Integrated G-019 nested/peer v1, G-021 optional/platform v2 and G-023 alias/workspace v3; [package contract](../../contracts/package-profiles.md#npm-pnpm-and-yarn), [native oracle](../../testing/packages.md), immutable `pkg.npm_resolution` and signed Content recovery v4. Apply `api-ui-design` and `research-and-validation` to the versioned profile and exact native differences. Design complete input/receipt identity and bounded graph projection before Main routes. |
| Owned paths | npm topology parser/projector/schema and offline oracle, narrow Main package route/client hunks, exact unit/real API/physical recovery fixtures and package contract/testing docs. Avoid Access Org/Realm transfer, Rating and Jena model owners. Manager serially integrates shared `app.ts`, generated OpenAPI and owner-cut fixtures. |
| Checks | Compare a registry alias under an incompatible optional platform, an alias shared by required and optional parents, a workspace target whose optional child is omitted, a workspace peer that remains required, a link whose target name differs from its directory, and a shadowing incompatible optional peer. Include Linux/Windows and x64/arm64 counterexamples, native virtual tree plus platform checker/optional-region evidence, and exact omitted instance/edge cause paths. Require unsupported or incomplete outcomes when the pinned native observation cannot prove identity; reject malformed/missing source bytes before projection. Demonstrate v1/v2/v3 immutable reads/replay, v4 same-key convergence, changed target/workspace bytes conflict, private/inactive denial, isolated Content physical recovery, and indexed reads at growing unrelated history. Bound bytes, nodes, edges, selectors, workspace paths, graph visits and ancestor lookups. Write tests with implementation; run documented selected `yarn test` paths, `yarn package:npm-oracle`, `yarn gen`, `yarn check:backend`, `yarn docs:check`; manager repeats merged checks. |
| Estimate | Initial 150–240 minutes after the combined projection invariant and native counterexamples are fixed. Report a v2/v3 semantic conflict before widening grammar. |
| Handoff | Commit one coherent worktree result; return commit, exact checks/artifacts, PKG04 claim and unresolved boundaries, then finish. No remote push or full backend QA. |

This package owner slice can run beside either active worker after a slot releases.
The manager integrates shared Main and recovery changes
serially. Every remaining backend Goal assertion stays retained.
