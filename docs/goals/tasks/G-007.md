# G-007 — Go 1.17+ module graph pruning

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 6 |
| State | Ready after merged G-005 checks; independent of G-004 Access consent |
| Base | Clean verified `main` after G-005 integration, in a fresh worktree |
| Deliverable | Add a versioned bounded captured Go profile for Go 1.17+ module graph pruning and lazy module loading. Resolve the selected build list from exact retained main and remote module manifests, and expose incomplete or unsupported evidence instead of applying the Go 1.16 unpruned traversal. Use the pinned native Go 1.27.1 oracle with generated local file proxy cases that distinguish pruned from unpruned behavior. Keep source capture identity, private immutable resolution, replay, and budget outcomes. |
| Acceptance | Partial PKG05/PKG12/PKG13 Go module semantics only. Live provider version sets, complete replace/exclude/retract combinations, lock verification and installation remain separate requirements. No full case is declared complete from this slice alone. |
| Dependencies | Integrated G-001 provenance and G-005 local replacement snapshot; official Go module reference and the pinned toolchain. Use `research-and-validation` for the pruning choice and verify exact behavior with the native oracle. Do not add a new resolver dependency or read arbitrary host paths in the API. |
| Owned paths | Package Go MVS/parser/owner modules, package oracle, package unit and PostgreSQL/Main API tests, package contracts and generated OpenAPI. The package section of `app.ts` is yours; G-004 owns Access/membership sections. Manager sequences shared generated OpenAPI. No Access migration or code. |
| Checks | Test with implementation: explicit versus transitive requirements, relevant `go` directives and lazy loading, version changes, missing retained manifests, incompatible captured metadata, bounded budget, private read/denial/replay and native differential outputs. Use documented affected `yarn test` paths, `yarn package:go-oracle`, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 90–120 minutes; report a semantic boundary or source-data gap promptly and keep other package requirements explicit. |
| Handoff | Commit on the isolated worktree branch, no remote push. Send commit, exact commands/results, IDs, remaining limits and generated-file note, then finish. |

The new profile must keep Go 1.16 behavior selectable. A pruned build list must
come from the native Go semantics and complete claimed evidence, not from simply
dropping unvisited modules from the existing result.
