# G-033 — npm override and engine policy profile

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 67 |
| State | Paused in its preserved independent worktree after former worker `/root/g033_npm_policy` was interrupted under the revised model policy. No live writer; awaiting an explicitly pinned Sol/xhigh worker slot or manager ownership. No PKG04 completion claim. |
| Base | Clean `main` at `6f0404f`, worktree `.temp/worktrees/g033-npm-policy`, branch `codex/g033-npm-policy`; independent of G-030 Access represented membership. |
| Deliverable | Add one separately versioned bounded npm lockfile-v3 policy profile for root overrides and engine compatibility using the pinned npm 11.19.1 virtual-tree oracle. Capture exact policy bytes, target platform, effective instance identity, override selection and engine result in an immutable receipt. Preserve v1–v4 request, result, ID and replay bytes. An unprovable native case must return a precise unsupported or incomplete outcome. |
| Acceptance | Advance PKG04/PKG12/PKG13 through exact native comparisons and real owner APIs. Do not claim installation, registry solving, broad workspace/override grammar, artifact validity, other package managers or PKG04 completion without the entire acceptance row. Explicitly retain any unsupported combinations. |
| Dependencies | Integrated G-019/G-021/G-023/G-026 npm profiles, [package contract](../../contracts/package-profiles.md#npm-pnpm-and-yarn), [native oracle](../../testing/packages.md), immutable `pkg.npm_resolution` and signed Content recovery. Apply `api-ui-design` and `research-and-validation` before fixing policy grammar and identity. Design owner schema/receipt and bounded projection before routes. |
| Owned paths | npm parser/projector/schema and offline oracle, narrow Main package route and client hunks, selected unit/native/real API/recovery fixtures, package contract/testing docs. Avoid Access membership/representation, Rating and Jena owners. Manager serially integrates shared `app.ts`, generated OpenAPI and owner-cut fixtures. |
| Checks | Include exact root override, incompatible transitive version, nested or alias override boundary, engines under explicit target Node/npm policy, absent/malformed override, same-key replay and changed-policy conflict. Compare exact native virtual-tree facts and platform evidence; unsupported cases must not become false success. Verify old profile immutability, private/inactive denial, physical owner-cut restore and indexed reads against unrelated history. Bound bytes, nodes, edges and graph visits. Write tests with implementation; use documented `yarn test` paths, `yarn package:npm-oracle`, `yarn gen`, `yarn check:backend`, `yarn docs:check`. Manager repeats merged affected checks. |
| Estimate | Initial 150–240 minutes after native semantics and receipt schema are fixed; report a blocking native disagreement before widening grammar. |
| Handoff | Commit one coherent independent-worktree result with exact checks/artifacts, claim and retained limits, then finish. No remote push or full backend QA. |

This separate package-owner slice is justified by parallel native-oracle and
PostgreSQL owner work while G-030 changes Access authority. Integration remains
serial. Every other backend Goal assertion stays retained.
