# G-030 — explicit representation versus eligible-member authority

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 58 |
| State | Ready for the next independent Access worktree slot |
| Base | Then-current verified `main` with G-024 Access move and G-027 IAM06; independent of G-028 Work/Source and G-029 Rating policy owners |
| Deliverable | Make the IAM25/IAM26 distinction executable for a bounded organization membership-administration operation. A principal who is only A's member, profile editor or administrator cannot represent A to administer B. A current explicit mandate to represent A for B's named membership action permits that operation only when A has the matching B grant, with the real principal retained privately. An eligible-member-set grant is a separate, explicitly named direct-principal selector; it can authorize a qualifying member without making that member A's representative. |
| Acceptance | Seek complete IAM25 and IAM26 candidates only after real Account/Access/Main API evidence distinguishes the denied implicit path, positive represented path, bounded eligible-set path and publishing-only/wrong-resource negative paths. IAM27 multihop representation, general selector grammar, arbitrary role/permission families, institutional delegation and host capacity remain retained. No membership, management relation, descriptive link or eligible-set grant can create representation. |
| Dependencies | Existing [representation/request contract](../../contracts/identity-and-access.md#representation-and-request-evaluation), G-012 Org/Realm admission, G-017 managed-grant separation, private membership and [IAM25/IAM26 acceptance](../../testing/identity-and-access.md). The existing `membership-changes` owner requires representation of its roster owner, and the existing request API issues only `work.create`; neither already proves A acting on B. Apply `api-ui-design` and `research-and-validation` before fixing the exact acting mode and eligible-set selector. Design Access owner schema, principal/Agent/resource identities, current proof and replay/cost bounds before routes. |
| Owned paths | Access representation, grant/selector and member-administration owner; narrow Main Access route/schema, real Account/Access/Main API and Access WAL fixtures, IAM contract/testing docs. Avoid G-028 Work title/Source and G-029 Rating owner files. Manager integrates shared `app.ts`, generated OpenAPI, recovery coverage and QA mapping serially. |
| Checks | P is admitted as A's member, profile editor and administrator but lacks a representation mandate: B membership administration is denied even when A has a B grant. Add an exact, valid A representation for B's membership action through an explicit request/change path: allow as A, record P privately, require B's recipient consent and current A grant. A publishing-only mandate, personal B grant pooled with A representation, wrong B resource, stale representation/role/grant generation, expiry, leave/rejoin, inactive principal, policy change and recovery hold deny new effects. A distinct eligible-member-set grant uses an explicit member selector and direct-principal mode without setting A as acting subject; it follows membership episode generations and never supplies representation. Concurrent same-key attempts converge, changed intent conflicts, lost response replays the same receipt. Preserve unrelated Realms, A/B operational rosters and no global control. Recover exact authority and receipts after isolated Access WAL restore; bound indexed probes, selected rows, writes and memory under unrelated history and a finite eligible set. Write tests with implementation; run documented selected `yarn test` paths, `yarn gen`, `yarn check:backend`, `yarn docs:check`; manager repeats merged checks. |
| Estimate | Initial 180–240 minutes after selector/acting-mode and authority transaction design; report a missing first-class membership-administration target or incompatible grant path early. |
| Handoff | Commit one coherent independent worktree result; return commit, exact checks/artifacts, IAM25/IAM26 claim and retained limits, then finish. No remote push or full backend QA. |

This Access operation is independent of the two current Work/Source and Rating
owner slices. The manager integrates shared routes and recovery coverage serially.
All remaining backend Goal assertions stay retained.
