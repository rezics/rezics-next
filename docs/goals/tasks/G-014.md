# G-014 — Two independent source supports for one native Work

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 21 |
| State | Prepared for the next Source slot after G-010 integration and one active handoff |
| Base | Clean verified `main` at dispatch, in a fresh worktree |
| Deliverable | Add a separately versioned Source operation to attach a second exact, verified title proposal to an existing native Work without rewriting that Work. Preserve the original adoption binding and receipt, record a distinct immutable support identity for the second SourceRecord, expose an authorized collection/read, and withdraw either support independently. Removing one support must leave the other support and the Work's independent accepted title/head intact. |
| Acceptance | Partial LIVE03/LIVE05/OPS03 evidence for two independent title supports and one withdrawal. General field-control arbitration, more than the bounded title profile, provider-wide withdrawal, child removal, rights/use decisions and cross-owner identity transfer remain separate. Do not close LIVE05 on this slice. |
| Dependencies | Merged G-010 title adoption/application/withdrawal, exact source proposal and graph receipt, current Work revision and edit mandate. Apply `api-ui-design` to fix the new attachment and collection wire contracts without changing the old singular read/withdrawal receipts; use `research-and-validation` for the conflicting support and recovery policy. Design Source owner schema, uniqueness, same-Work attachment, per-binding locks and recovery coverage before API edits. |
| Owned paths | Source owner migration after 017; source attachment/support module and route/schema hunks in Main `app.ts`; generated OpenAPI; Account/Access/Main/PostgreSQL API and held-graph or physical recovery tests; source contracts/testing docs. Avoid Access and Cargo owner code. Coordinate shared generated files with manager. |
| Checks | Write tests with implementation. Two distinct complete SourceRecords/proposals may support the same current Work title under an exact active Work edit mandate and expected native head; a duplicate proposal or changed key cannot create another binding. Attaching a source with a different title, another Work, stale head, different principal or weak authority cannot claim an accepted native title. The new collection is private and deterministic; existing adoption/singular read and v1 withdrawal replay exactly. Withdrawal of support A leaves B, the native Work ID/title/head and a later human same-value head intact; B withdrawal cannot resurrect A. Concurrent attach/withdraw, pending title intents, source graph mismatch, principal deactivation, restore/replay and indexed bounded reads are explicit. Run documented affected `yarn test` paths, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 120–180 minutes; report a structural schema/receipt conflict before widening the title profile. |
| Handoff | Commit in the isolated worktree, no remote push. Send manager commit, exact checks/run IDs, remaining LIVE boundaries and generated-file note, then finish. |

The attachment acknowledges the second source's title against a current native
Work head. It does not let Source create or edit that head, infer consent from a
matching value, grant publication authority, or treat a missing source field as a
withdrawal. Preserve old immutable receipts and keep each new disposition tied
to its own source binding.
