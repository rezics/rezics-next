# G-012 — Independent organization participation in a Realm

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 11 |
| State | Prepared for an independent Access slot after G-009 integration and one active handoff |
| Base | Clean verified `main` at dispatch, in a fresh worktree |
| Deliverable | Add an Access-owned independent Org-to-Realm participation episode, separate from `kind: org` operational roster and `kind: realm` Agent membership. One current tuple, immutable history and receipts, exact generation and policy/terms basis, an independent ban, and two-sided admission must govern join, leave and rejoin. The Realm's decision can admit or suspend participation in that Realm only; an organization representative must separately assent to its participation. A structural link never grants Realm control of the organization, source ownership, publication/edit rights, representation, or participation in another Realm. |
| Acceptance | Partial IAM23/IAM24 and IAM06 evidence for independent Org participation and suspension. Explicitly managed mode, Realm-local publication moderation, move semantics, paid benefits, quota/review and complete capacity remain later requirements; this task cannot declare IAM23/24 complete. |
| Dependencies | Merged G-009 Access owner, [Realm participation contract](../../contracts/realm-participation.md), Account-verified representation/grant ceilings and the existing membership policy/consent pattern. Apply `api-ui-design` and `research-and-validation` to define the two-party API and exact authority receipt before code. Design Access owner schema, uniqueness and lifecycle guards before implementation. Do not reuse an Agent roster tuple as the organization-to-Realm identity. |
| Owned paths | New Access participation migration after 024, Access owner modules and participation route/schema hunks in Main `app.ts`/index, generated OpenAPI, real Account/Main/Access/PostgreSQL integration and isolated Access recovery tests, identity/Realm contracts and testing docs. Avoid Source and Cargo changes; manager sequences shared generated files. |
| Checks | With implementation tests: independent Org and Realm managers have distinct verified mandates and ceilings; a single side cannot self-admit the other; exact proposal/consent generation, policy and terms are checked at commit; retry, stale, concurrent competing join and suspension are explicit; leave/rejoin does not revive authority; another Realm and the Org's own operational roster survive; no management, source or publication authority appears from participation; Account deactivation and recovery hold deny new effects; bounded indexed work and WAL restore preserve current tuple and immutable history. Run documented affected `yarn test` paths, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 105–165 minutes after two-party authority contract design; report a missing mandate/ceiling or owner-schema conflict before widening scope. |
| Handoff | Commit on isolated worktree, no remote push. Send manager commit, exact checks/run IDs, unresolved IAM boundaries and generated-file note, then finish. |

This first episode represents independent participation only. It does not
activate managed-organization authority, alter native publication receipts or
implement a general Realm move as leave plus join without its own qualified
cross-Realm transition.
