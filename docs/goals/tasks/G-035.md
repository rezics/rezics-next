# G-035 — exact semantic value round-trip for one Work profile

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 80 |
| State | Ready for one explicitly pinned Sol/xhigh worker on an independent worktree from clean `main` after G-034. MODEL02 remains partial. |
| Deliverable | Establish one Work-owned semantic value profile and real API write/read/export path that preserves zero, false, empty string, omitted/absent, explicit unknown and explicit no-value as distinct states. Make one complete MODEL02 case with exact revision, retry, stale, denial and recovery evidence if the existing owner boundaries permit; otherwise return the narrowest concrete blocker and verified partial result. |
| Inputs | [Model acceptance](../../testing/model-contracts.md), [semantic model contract](../../contracts/semantic-model.md), existing Work command/revision infrastructure, Main API and Account/Access authorization. `POST /v1/semantic/changes` and exact semantic resource revision reads are planned, not already callable. Preserve existing Work and Content semantics and released revision anchors. |
| Schema first | Determine the owning RDF/immutable manifest shape and API sum type before routes. Each variant needs a canonical wire encoding, allowed lexical value, omitted/null rule, revision identity and export representation. Distinguish unknown from a known lack of value and from missing property. Do not use JavaScript truthiness, JSON number conversion or an untyped null sentinel to collapse states. A new PostgreSQL table needs a specific ownership reason; Main/Jena and immutable revision objects already own Work state. |
| Real template gate | Verify one actual Account/Access/Main/Jena write and exact read through the root harness before adding repetitive scalar variants. Include one denied or stale API request in that gate. No mock-only completion claim. |
| Acceptance | The six MODEL02 states survive write, current read, exact old-revision read, query and export with distinct round-trip observations. An edit changes only the selected Work/property revision, an identical key replays once, changed intent conflicts, stale expected head fails, and unauthorized actors cannot write or read private state. Missing/corrupt immutable bytes yield typed unavailable rather than current-head substitution. Include graph-loss recovery and bounded path checks, or state explicitly which part prevents a complete-case claim. Preserve all MODEL03/MODEL04 and general semantic-change obligations as separate backlog. |
| Owned paths | New semantic owner module/schema/shape, narrow Main routes and schemas, corresponding real tests, model contract/testing docs. The manager serially owns `generated/openapi/main/public.json`, QA complete-case coverage mapping, shared QA scheduling, plan and final merge. Avoid Access, package, Rating and Source migrations. |
| Design method | Apply `api-ui-design` and `research-and-validation` for the semantic/API choice. Use applicable primary RDF/JSON and existing repo evidence, compare credible encodings, retain decisions and limits beside the model contract. No frontend implementation. |
| Checks | Tests with implementation. Use documented affected `yarn test` paths, `yarn gen`, `yarn check:backend`, `yarn docs:check`. Coordinate the shared QA stack with the manager before a real integration/recovery run; never run full QA or public-command seed. Routine preparation limit is 600 seconds. |
| Model and isolation | Sol/xhigh because this is a new semantic owner, cross-owner API and recovery boundary. Astra is forbidden. The independent worktree isolates shared Main routes and generated schema. A later bounded repetitive expansion may use Luna/max only after the schema and first real API template pass. |
| Estimate | Initial 90–150 minutes for owner profile and real proof; report an immutable-revision or authority mismatch before broadening the grammar. The 13:57:20 UTC ten-hour target is already forecast to miss. |
| Handoff | Commit one coherent worktree result and finish. Return commit, exact tests/artifacts, case claim or blocker, cost/recovery limits and next integration action. No remote push. |

The split is justified by an independent semantic owner and reusable value
template; the manager can qualify existing Search owners without overlapping
files or the shared QA stack. Every other backend Goal assertion remains
retained.
