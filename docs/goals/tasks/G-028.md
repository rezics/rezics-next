# G-028 — Work title editorial-control epoch

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 51 |
| State | Clean worktree handoff `ed05b21` in manager integration queue; original task `01a0dd21-6f4e-7950-b4e0-a4efc2929e24` was interrupted and recovery worker completed it |
| Base | Clean `main` at `003975d`, worktree `/home/edge/.codex/worktrees/aad1/rezics-next`; Work-title command/model ownership is independent of G-025 Rating aggregation and G-026 npm composition. |
| Deliverable | Add the first owner-enforced Work title `EditorialControlRevision` profile to the existing title edit/source-application path. A source-managed title binds its exact source observation, mapping, target head and monotonic control epoch. Any admitted human title edit, including equal bytes, creates a human-controlled successor epoch. A later source application cannot overwrite that head or infer a return to source control. An explicitly authorized, exact-basis return to source control is a distinct operation; ordinary edit or source scope cannot forge it. |
| Acceptance | Advance partial LIVE01/LIVE03/MODEL17 and editorial-protection foundations through real Account/Access/Main/Source/Jena owners. The complete LIVE03 and generic review-required/sealed protection rows remain retained until every specified target, writer, race, recovery and capacity boundary is qualified. Do not call a title-only control profile general field control or permission to reuse source expression. |
| Dependencies | Existing Open Library Work observation/proposal/adoption, title support, refresh assessment, guarded source title application, human Work edit and withdrawal; [source lifecycle](../../contracts/source-lifecycle.md), [editorial protection](../../contracts/editorial-protection.md) and [required owner scenarios](../../testing/editorial-protection.md). Apply `api-ui-design` and `research-and-validation` to target identity, current-head/control-epoch CAS, trusted origin and distinct authority. Design owner/model schema, immutable recovery envelope and bounded access path before API routes. |
| Owned paths | Work metadata/title command and model, Source title application and exact support binding, narrow Main Source/Work routes, selected native/model/real API/graph-loss fixtures and owner contracts/testing docs. Prefer a focused Source fixture over unrelated package hunks in the shared `source-authenticated-api.test.ts`. Avoid G-026 package owner and G-025 Rating implementation. Manager serially integrates shared `app.ts`, generated OpenAPI/model artifacts and recovery fixtures. |
| Checks | Race equal-value human edit and source application at one expected content/control head with barriers. If human wins, source returns conflict without a new native title; if source wins, later human confirmation advances control even for equal bytes. A newer proposal cannot reapply over that human-controlled head. Missing protection/control expectation and forged origin are denied inside the owner command; unrelated historical source observations remain readable. An explicit source-control return requires a separate current Access action and exact eligible SourceRecord/observation/mapping, cannot release a later protection or restore a withdrawn/restricted support, and cannot reuse a stale decision. Verify immutable exact revisions, same-key replay, changed-key conflict, lost response, concurrent return attempts, graph-loss recovery and bounded indexed reads, graph calls/bytes and writes under unrelated histories. Write tests with implementation; run documented selected `yarn test` paths, relevant model/native checks, `yarn gen`, `yarn check:backend`, `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 180–240 minutes after the title control CAS and return authority are fixed; report an unresolved cross-owner fence or protection-head ambiguity before expanding the profile. |
| Handoff | Commit one coherent independent worktree result; return commit, exact checks/artifacts, acceptance claim and retained boundaries, then finish. No remote push or full backend QA. |

This first title-control profile is a bounded foundation for the retained
editorial-protection contract. It must keep every other field, correction,
rights, capacity and recovery assertion in the backend Goal.
