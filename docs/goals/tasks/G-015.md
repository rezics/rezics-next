# G-015 — Realm daily Rating observation and server calendar

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 22 |
| State | Prepared for the next independent Rating/model slot after one active handoff |
| Base | Clean verified `main` at dispatch, in a fresh worktree |
| Deliverable | Add separately versioned Realm daily RatingContext and observation operations. The server determines the civil day from a pinned IANA timezone and trusted submission instant, records exact DST-resolved period bounds, and derives a private one-Account-principal slot for that Context/MainVersion/day. Correction and withdrawal revise that one observation under an exact head; a new day creates a new observation. Public persona changes cannot multiply a principal's vote. |
| Acceptance | Seek complete RATE03 and partial RATE02 evidence after merged real-owner checks; final recorded backend QA decides full case status. Standing RatingContext/Observation and RATE04 receipts must replay unchanged. Experience cadence, broader aggregation policies and all event-time cases remain separate. |
| Dependencies | Current standing Rating owner, Access `rating.observation.set` and private principal identity, native command/SHACL compiler and retained recovery. Apply `api-ui-design` and `research-and-validation` to the civil-day, timezone, clock and wire contract. Design the versioned model profile, immutable manifest/slot/revision identity, indexes and cost bound before API edits; add a tool/library through `docs/development/toolchain.md` first if required. |
| Owned paths | Rating context/observation modules and model profile/definitions; Main Rating route/schema hunks and generated OpenAPI; native Jena command if needed; Rating and temporal contract/testing docs; real Account/Access/Main/Jena API, model and recovery tests. Avoid Source, Cargo and Access owner changes unless a narrow interface repair is necessary. Manager sequences shared `app.ts`/generated files. |
| Checks | Tests with implementation: a server period around spring-forward and fall-back has one correct local date with exact UTC bounds; client offset, clock or persona cannot select another slot; same private principal/context/target/day converges under concurrent first submissions and idempotent retry; another day and another principal are distinct; correction and withdrawal retain exact revision history and never resurrect a prior opinion; inactive principal, stale head and missing authority deny writes; standing receipts remain byte-for-byte compatible; graph-loss restore replays the daily manifest/slot without duplicates. Declare complete RATE03 only if the real owner and API fixture covers its full retained scenario. Run documented affected `yarn test` paths, relevant model/backend checks, `yarn check:backend` and `yarn docs:check`; manager repeats on merged source. |
| Estimate | Initial 150–240 minutes after calendar/model contract design; report a native command or clock-source blocker before widening scope. |
| Handoff | Commit in the isolated worktree, no remote push. Send manager commit, selected checks/run IDs, exact RATE03 coverage claim, remaining Rating boundaries and generated-file note, then finish. |

The client may submit a value or an exact-head revision intent, but it cannot
author the day key or choose a public persona as the counting identity. A daily
context and a standing context are distinct questions and receipt profiles.
