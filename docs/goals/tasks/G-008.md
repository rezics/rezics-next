# G-008 — Cargo resolver snapshot and native oracle

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 7 |
| State | Ready after integrated G-007; independent of G-006 Access membership |
| Base | Clean verified `main` after G-007 integration, in a fresh worktree |
| Deliverable | Establish the first bounded Cargo package-resolution profile using exact caller-supplied manifest/registry snapshot bytes and an isolated pinned native Cargo oracle. Preserve original package/source identity, resolver version, target/host roles, requested/default/optional features and the native selected instance graph. Return explicit unsupported, incomplete and budget outcomes; do not convert a graph to a name-to-one-version map. Store an immutable private resolution with exact read and replay through Main. |
| Acceptance | Partial PKG01/PKG02/PKG12/PKG13 Cargo evidence. Native `links` conflict and yanked lock eligibility must be modeled only when this profile explicitly supports and proves them; otherwise return unsupported. This task cannot claim full package acceptance, installation or build success. |
| Dependencies | Existing package owner/API patterns and official Cargo resolver reference. Use `research-and-validation` and `api-ui-design` for the exact bounded profile and source identity. The current toolchain names Rust/Cargo 1.98.1 for the upstream bridge only: update `docs/development/toolchain.md` and add documented root oracle command before using Cargo for this new purpose. Do not install or invoke an unpinned package tool. |
| Owned paths | New package Cargo adapter/owner and migration, package app.ts sections, generated OpenAPI, native oracle script and root facade, unit and real PostgreSQL/Main API tests, package contract/testing/toolchain docs. G-006 owns separate Access and membership app.ts sections; manager sequences generated OpenAPI. Avoid modifying Access migrations or policy. |
| Checks | Test with implementation: native Cargo differential over a small dependency graph with feature unification and target/host distinction, private OAuth/Access/API denial and replay, malformed/partial snapshot, unsupported clause, budget, duplicate or changed identity, and immutable exact read. Use documented affected `yarn test` paths and `yarn check:backend`; manager repeats on merged source. |
| Estimate | Initial 90–150 minutes including toolchain and API contract design; report any native-oracle or captured-registry blocker promptly. |
| Handoff | Commit on isolated worktree branch, no remote push. Send manager commit, paths/IDs, commands/results, semantic limits and generated-file note, then finish. |

Resolve a controlled graph through an admitted native Cargo profile. Keep
manifest parsing, logical resolution, physical installation and build results
as separate claims.
