# G-029 — Rating question identity versus aggregate policy revision

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 56 |
| State | Running in independent worktree task `01a0dd2b-024a-7882-b412-48302629f5e7` |
| Base | Verified G-025 RATE01/02/03 owner on clean `main` at `5d533df`, worktree `/home/edge/.codex/worktrees/7457/rezics-next`; independent of G-028 Work-title Source control |
| Deliverable | Implement an explicit immutable RatingContext aggregate-default policy revision and a real context-default aggregate consumer. Changing the question, scale, target grain, population or cadence creates a distinct Context through the existing create API; a reduction-default change retains the Context, observations and votes while advancing an exact policy-revision head. The default consumer resolves that head and names the revision and reduction used. Existing explicit RATE01 profile requests remain byte-compatible and continue to use their named policies. |
| Acceptance | Seek a complete RATE05 candidate only if a real Account/Access/Main/Jena path proves the question-versus-policy distinction. Two Contexts with different questions must never share observation identity or votes. For A's 2,2,8 and B's 6, an explicit revision from latest-per-rater to mean-per-rater changes the context-default aggregate from 7 to 5 while the separately named latest, mean and pooled profiles still return 7, 5 and 4.5. No rewrite of historical Context/Observation manifests or silent relabel of a prior response. RATE06 cross-Realm/global synthesis, calendar/event time, materialized generations, general governance and deployment capacity remain retained. |
| Dependencies | Integrated G-025 private bounded aggregate inventory and three explicit reductions; [Rating contract](../../contracts/ratings.md#aggregation-policy) and [RATE05 row](../../testing/ratings-and-event-time.md). Apply `api-ui-design` and `research-and-validation` before fixing policy revision identity, default resolution, exact authority and query consistency. Design owner/model schema and guarded policy-head command first. Use only documented toolchain root commands. |
| Owned paths | Rating Context policy revision/model and reduction selection, narrow Main Rating routes/schema, related Access admission action only if required, focused native/model/real API/graph-loss tests and Rating contract/testing docs. Avoid G-028 Source/Work title modules and npm package owner. Manager serially integrates shared `app.ts`, generated OpenAPI/model files, recovery coverage and registered QA mapping. |
| Checks | A new question with identical numeric scale creates a different Context; policy mutation cannot change question or repurpose existing observations. Current authorized configuration with exact expected policy head creates one immutable successor; stale head, different intent under a reused key, concurrent setters, inactive principal, missing permission, recovery hold and lost response have explicit one-effect outcomes. An authorized exact read returns both policy revisions and their immutable basis. Context-default aggregation resolves a single graph/policy snapshot with explicit revision and denominator, while each explicit old profile stays stable. Context/Observation replay and graph-loss restore recover the exact policy head and output; a missing or rolled-back policy revision is unavailable, not defaulted to the old reduction. Bound policy-head lookups, graph calls/bytes, immutable manifests and write amplification independently of unrelated Contexts and revision history. Write tests with implementation; run documented selected `yarn test` paths, relevant native/model checks, `yarn gen`, `yarn check:backend`, `yarn docs:check`; manager repeats merged checks. |
| Estimate | Initial 180–240 minutes after policy head, default consumer and authority contract are fixed; report any ambiguity over baseline policy revision or Access action before widening scope. |
| Handoff | Commit one coherent independent worktree result; return commit, exact diagnostics/artifacts, RATE05 claim and remaining limits, then finish. No remote push or full backend QA. |

This Rating governance slice is independent of the Source/Work title-control
worker. The manager integrates shared Main and generated artifacts serially.
All other backend Goal assertions remain retained.
