# G-021 — npm optional and platform topology

| Field | Assignment |
| --- | --- |
| Parent/revision | Backend [GOAL](../../../GOAL.md), orchestration revision 35 |
| State | Ready after G-019 merged API and physical recovery checks |
| Base | Clean `main` at dispatch; independent worktree required |
| Deliverable | Add a separately versioned bounded npm lockfile-v3 topology profile that retains optional dependency and OS/CPU selector meaning for one declared target environment. Compare its admitted tree and omission reasons against pinned npm 11.19.1's offline virtual-tree interpretation. Preserve exact manifest/lock bytes, source and integrity identity, installed path and peer-host environment; distinguish a platform-inactive optional instance from missing required source or an incompatible required peer. Keep G-019 v1 reads, receipts and replay unchanged. This profile validates an exact caller-supplied lock, and does not claim fresh range solving or installation. |
| Acceptance | Partial PKG04/PKG12/PKG13 evidence with explicit native counterexamples. Alias, workspace, override and engine behavior, pnpm/Yarn strategies, live registry resolution, artifact verification, full installation and final complete-case qualification remain retained later work. Do not close PKG04 from a parser or a single platform happy path. |
| Dependencies | G-019 integrated owner/API, migration 020, signed Content recovery v4 and [npm profile contract](../../contracts/package-profiles.md#npm-pnpm-and-yarn). Use the documented `yarn package:npm-oracle` and pinned npm 11.19.1. Apply `research-and-validation` and `api-ui-design` to the native interpretation and wire version before coding. Design the owner identity, immutable receipt/recovery compatibility and target-environment fields first. The existing raw-byte/digest request is the source of truth; do not infer an observed installed tree from a lock alone. |
| Owned paths | npm parser/topology/resolution/schema and fixed offline oracle/fixtures, narrow Main route/client hunks if a new endpoint is justified, affected package owner migration only if the existing immutable row cannot carry the versioned receipt, selected unit/real API/physical restore tests, package contracts and testing docs. Avoid G-020 Access/Realm/Work modules and Jena/Source owner changes. Manager sequences shared `app.ts`, generated OpenAPI and recovery integration after handoff. |
| Checks | Write tests with implementation. Native npm and the versioned profile agree on at least Linux and Windows target cases with one optional package enabled on only one target, an optional transitive dependency, an omitted optional peer, a required dependency that cannot be silently omitted, and incompatible peer shadowing. Compare exact paths, versions, source/SRI and peer hosts or explicit omission reasons, not package counts alone. Unknown target, malformed selector, unsupported alias/workspace/override, incomplete lock and resource budget must retain distinct truthful outcomes. Existing v1 request/receipt/replay and all eight native G-019 scenarios stay byte-for-byte compatible. Prove private exact replay, changed-key conflict and inactive-principal fencing through the real API; recover old and new npm receipts with existing Go/Cargo receipts after an isolated physical cut. Keep bounded bytes/nodes/edges/ancestor lookups and indexed receipt reads as unrelated history grows. Run documented affected `yarn test` paths, `yarn package:npm-oracle`, `yarn gen`, `yarn check:backend` and `yarn docs:check`; manager repeats selected checks on merged source. |
| Estimate | Initial 150–240 minutes after native counterexamples and owner schema are fixed; report any npm optional/peer ambiguity before widening the grammar. |
| Handoff | Commit one coherent result on the worktree branch and finish with commit hash, changed owners, exact commands/artifacts, unresolved boundaries and required manager integration. No remote push or full backend QA. |

The independent package boundary supports a second worker while G-020 owns
Realm admission and moderation. The native comparison must state which target
environment it models; an absent optional instance is not proof of an invalid
required graph or successful installation.
